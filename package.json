{
  "name": "fe_mock_project_trihn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "lint:fe": "pnpm --filter fe lint",
    "lint:be": "pnpm --filter be lint",
    "lint": "pnpm lint:fe && pnpm lint:be"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^14.0.1"
  },
  "lint-staged": {
    "config": "lint-staged.config.js"
  }
}